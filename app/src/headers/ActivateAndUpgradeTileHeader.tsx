import { HeaderText } from '@gamepark/react-game'

/**
 * A Shark card activating a tile out of turn: the tile is picked on the table, on the tile itself, and it is
 * upgraded right after if it can be (see {@link ActivateAndUpgradeTileRule}).
 */
export const ActivateAndUpgradeTileHeader = () => <HeaderText code="activate-and-upgrade-tile" />
