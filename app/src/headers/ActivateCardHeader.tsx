import { HeaderText } from '@gamepark/react-game'

/**
 * The Panda Queen activates another of the cards in play, which is picked on the table, on the card itself
 * (see {@link ActivateCardRule}).
 */
export const ActivateCardHeader = () => <HeaderText code="activate-card" />
