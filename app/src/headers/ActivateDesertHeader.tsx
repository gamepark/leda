import { HeaderText } from '@gamepark/react-game'

/**
 * A Scorpion card reading the effect reminded on a Desert: the Desert is picked on the table, on the tile itself,
 * and it stays a Desert once it has given what it reminds (see {@link ActivateDesertRule}).
 */
export const ActivateDesertHeader = () => <HeaderText code="activate-desert" />
