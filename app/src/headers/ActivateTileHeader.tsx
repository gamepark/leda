import { HeaderText } from '@gamepark/react-game'

/**
 * A Cat card activating a tile out of turn: the tile is picked on the table, on the tile itself, and nothing else
 * happens to it (see {@link ActivateTileRule}).
 */
export const ActivateTileHeader = () => <HeaderText code="activate-tile" />
