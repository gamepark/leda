import { HeaderText } from '@gamepark/react-game'

/** The player resolves what one of their own Snakes gives, that Snake being left exactly as it stands. */
export const CopySnakeHeader = () => <HeaderText code="copy-snake" />
