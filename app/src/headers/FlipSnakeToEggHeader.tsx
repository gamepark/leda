import { HeaderText } from '@gamepark/react-game'

/**
 * The player turns one of their Snakes back onto its Egg side, which is what the 2 strongest cards of the clan
 * are paid with rather than a bonus they give (see {@link FlipSnakeToEggRule}).
 */
export const FlipSnakeToEggHeader = () => <HeaderText code="flip-snake-to-egg" />
