import { HeaderText } from '@gamepark/react-game'

/** The player lays one of their Eggs on another square of their grid, which is done by dragging it there. */
export const MoveEggHeader = () => <HeaderText code="move-egg" />
