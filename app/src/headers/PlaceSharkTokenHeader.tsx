import { HeaderText } from '@gamepark/react-game'

/** Placing a Shark token on a tile that has none, which is answered on the tile itself. */
export const PlaceSharkTokenHeader = () => <HeaderText code="place-shark-token" />
