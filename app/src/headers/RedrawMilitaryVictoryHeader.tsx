import { HeaderText } from '@gamepark/react-game'

/** Trading a Military Victory token for the next of the pile, which is answered on the token itself. */
export const RedrawMilitaryVictoryHeader = () => <HeaderText code="redraw-military-victory" />
