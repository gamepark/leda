import { HeaderText } from '@gamepark/react-game'

/**
 * A Scorpion Portal swapping 2 squares: answered by dragging one square onto another, the same way an organisation
 * is, so there is nothing to offer here beyond saying so (see {@link SwapSquaresRule}).
 */
export const SwapSquaresHeader = () => <HeaderText code="swap-squares" />
