import { HeaderText } from '@gamepark/react-game'

/** Resolving one of the Military Victory tokens already won, which is answered on the token itself. */
export const TriggerMilitaryVictoryHeader = () => <HeaderText code="trigger-military-victory" />
