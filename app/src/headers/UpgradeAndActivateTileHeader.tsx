import { HeaderText } from '@gamepark/react-game'

/**
 * A Scorpion card upgrading a tile and activating it right after: the tile is picked on the table, and what it
 * gives is what its upgraded face gives (see {@link UpgradeAndActivateTileRule}).
 */
export const UpgradeAndActivateTileHeader = () => <HeaderText code="upgrade-and-activate-tile" />
