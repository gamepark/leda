/**
 * Where a button sits on a square of a grid, in centimeters from the center of the tile: tucked into its top left
 * corner, a bit inside the 7 cm tile, so that it leaves the bottom of the artwork, where the symbols of the tile
 * are read, and its top right corner, where the badge of a permanent tile is printed.
 */
export const tileButtonPosition = { x: -2.2, y: -2.2 }
